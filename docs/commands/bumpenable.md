# `!bumpenable`

Active/désactive le système de bump (choix du salon).

- **Catégorie** : 🎲 Utilitaire
- **Permission** : Aucune
- **Disponible en** : préfixe (`!`) et slash (`/`)
- **Usage** : `!bumpenable [etat] [salon]`

> Légende : `<...>` argument obligatoire · `[...]` facultatif.
